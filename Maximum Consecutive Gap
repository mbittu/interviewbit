int Solution::maximumGap(const vector<int> &A) 
{
    vector<int>a(A.size());
    for(int i=0;i<A.size();i++)
        a[i]=A[i];
    sort(a.begin(),a.end());
    int max=0;
    for(int i=0;i<A.size()-1;i++)
    {
        if((a[i+1]-a[i])>max)
            max=a[i+1]-a[i];
    }
    return max;
}
